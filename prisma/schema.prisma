generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./db.sqlite"
}

model Todo {
  id          String   @id @unique @default(uuid())
  title       String
  description String?
  completed   Boolean? @default(false)
  user        String
}
